با آدرس http://localhost:3000/user میتوان تمام کاربران را دید ، انهارا حذف کرد یا ویرایش کرد


با آدرس http://localhost:3000 میتوان برای ثبت نام یا ورود اقدام کرد

با آدرس http://localhost:3000 میتوان برای ثبت نام یا ورود اقدام کرد

برای ثبت نام نباید email موجود باشد همچنین رمز عبور نیز باید بین 5 تا 10 کاراکتر باشد

با استفاده از پکیج session توکن ها در دیتابیس mongo ذخیره میشن  و سپس برسی در صفحه اصلی اگر کاربر session معتبری داشت میتونه به داشبورد دستری داشته باشه در غیر این صورت باید ورود یا ثبت نام انجام بده

برای ورود هم email , password باید معتبر باشند

در صفحه داشبورد کاربر میتونه اطلاعاتی مثل اسم و پروفایل خودشو اپدیت کنه  و یا از اکانتش 
خارج بشه

از express-validator برای برسی ورودی های ارسالی توسط کاربر استفاده شده 

در این پروژه از پکیج های passport , bcrypt برای هندل کردن ثبت نام و ذخیره رمز کاربر به صورت hash شده در دیتابیس استفاده شده است